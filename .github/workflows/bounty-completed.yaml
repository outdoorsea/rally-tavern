name: Bounty Completed

on:
  push:
    paths:
      - 'bounties/done/*.yaml'

jobs:
  celebrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Log completion
        run: |
          echo "✓ Bounty completed!"
          git diff --name-only HEAD~1 HEAD -- 'bounties/done/*.yaml'
